import { Data } from './types';

// data structure for id
const Id = {
  quizId: 0,
  questionId: 0,
};

// data structure for quizzes
const data: Data = {
  quizzes: [],
  questions: [],
};

export { data, Id };
